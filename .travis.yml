language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pedro412/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pedro412/docker-react npm run test
